# Optional: OpenAI API Key for real LLM responses
# Get your key from https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=

# Optional: Configure to use real APIs instead of mock data
# Set USE_LOCAL_DATA=false in src/api/apiClient.js

# App runs fine without any API keys - uses local mock data!
